config:
  aws:region: us-west-2
  # distributionArn is optional; when set, the publisher role's
  # cloudfront:CreateInvalidation is scoped to just that distribution instead of "*".
  # pulumi-docs-versioned:distributionArn: "arn:aws:cloudfront::571684982431:distribution/XXXXXXXXXXXXX"
