# This file contains programs that should be ignored by the example code test script.

# This also supports grep compatible regex patterns so we can ignore all programs that match
# a given pattern. You can also include comments in the file to explain why the program is being ignored.

# No need to include a leading anchor (^) or trailing EOL ($) as these are handled by the script to support
# matching directories listed specifically.

# Example:
# aws-.* will ignore all programs that start with "aws-"
#

# Java SDK serialization bug: FargateService container/containers inputs
# not received by AWSX provider. https://github.com/pulumi/pulumi-java/issues/2090
awsx-load-balanced-fargate-nginx-java
awsx-vpc-fargate-service-java

# API Gateway authorizer parameter `providerArns` is mistyped.
# https://github.com/pulumi/pulumi-aws-apigateway/issues/121
awsx-apigateway-auth-cognito-java

# Import tutorial programs that are not currently consumed by any docs page
# and require pre-existing IAM resources the CI account does not have.
aws-import-iac-iam-role-.*

# Custom-domain examples won't work because of the hosted-zone lookup (which will fail unless
# the credentials can access the specified Route 53 zone).
awsx-apigateway-custom-domain-.*

# CSI driver examples require a `github-pat` config value to pull a private
# image/repo; CI does not provide one. The Go variant does not need it and
# is tested. https://github.com/pulumi/docs/issues/14505
kubernetes-csi-driver-csharp
kubernetes-csi-driver-python
kubernetes-csi-driver-typescript

# Requires a local GPU node, model download, and Tailscale credentials that CI
# does not provide.
self-host-gemma4-llm-python

# Azure YAML import test: the engine requires import to be a literal
# string, but the YAML program needs interpolation for the resource ID.
azure-native-import-resource-group-yaml

# GCP import tests: service account doesn't exist yet and GCP project
# configuration needs to be sorted out. https://github.com/pulumi/docs/issues/14505
gcp-import-service-account-.*

# All JS examples were removed from static programs.
# TODO: See if this line can be removed once the code has been merged to master
and the tests have been run.
*-javascript

# Static website programs require Hugo at runtime (subprocess.run), which
# intermittently crashes (SIGABRT) in CI due to resource contention.
aws-static-website-with-runtime-logic-.*

# Terraform reference project: intentionally not a Pulumi program (used as
# an external tfstate source by other examples).
tf-state-ref-terraform
