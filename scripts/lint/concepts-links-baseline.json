{
    "_note": "Ratchet baseline for scripts/lint/check-concepts-links.js. PR #21138 and PR #21145 (the cleanup pass this guard exists to protect) both merged on 2026-08-26, so every internal /docs/concepts/* link this baseline used to track is fixed on master and the allowance list below is empty. Do not add an entry to silence a real finding -- fix the link instead. If the guard ever needs an exception again, that's a real, justified allowance for a specific file, not a place to park unrelated cleanup debt. A file's count going lower than what is recorded here only prints a warning, never fails; going higher fails the build.",
    "files": {}
}
