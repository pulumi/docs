---
title: "esc env provider gcp-login static | CLI commands"
aliases:
  - /docs/reference/cli/esc_env_provider_gcp-login_static/
meta_desc: "Learn about the esc env provider gcp-login static command."
---



Add a GCP static-credentials login provider to an environment

## Synopsis

[EXPERIMENTAL] Add a GCP static-credentials login provider to an environment

Writes an `fn::open::gcp-login` block at the configured path under `values`. The
access token is wrapped in `fn::secret`. <project-number> must be the numerical
GCP project ID. If a block already exists at the path it is replaced.

See https://www.pulumi.com/docs/esc/integrations/dynamic-login-credentials/gcp-login/
for the full provider reference.


```
esc env provider gcp-login static [<org>/][<project>/]<environment-name> <project-number> <access-token> [flags]
```

## Options

```
      --create                   create the environment if it does not already exist
      --draft string[="new"]     set flag without a value (--draft) to create a draft rather than saving changes directly. --draft=<change-request-id> to update an existing change request.
  -h, --help                     help for static
      --path values              property path under values where the provider block is written (default "gcp.login")
      --service-account string   optional GCP service account to impersonate
      --token-lifetime string    optional lifetime for impersonated credentials, e.g. 1h30m
```

## Options inherited from parent commands

```
      --env string   The name of the environment to operate on.
```

## SEE ALSO

* [esc env provider gcp-login](/docs/esc/cli/commands/esc_env_provider_gcp-login/)	 - Add a GCP login provider to an environment

###### Auto generated by spf13/cobra on 1-Jun-2026
