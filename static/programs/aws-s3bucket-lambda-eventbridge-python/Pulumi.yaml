name: aws-s3bucket-lambda-eventbridge-python
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: A example that deploys an S3 bucket, lambda function resources, and an eventbridge trigger permission.
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
