name: aws-s3-bucket-resources-python
runtime:
  name: python
  options:
    virtualenv: venv
description: An example that deploys S3 bucket, metric, notification, object, and policy resources.
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
