name: kubernetes-deployments-javascript
description: An example that creates a Kubernetes Deployment
runtime:
  name: nodejs
  options:
    typescript: false
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-javascript
