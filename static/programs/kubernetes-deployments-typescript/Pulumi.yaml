name: kubernetes-deployments-typescript
description: An example that creates a Kubernetes Deployment
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-typescript
