name: azure-providers-go
description: An example that uses the Azure Native and Azure AD providers together to create a resource group and an Entra ID service principal.
runtime: go
config:
  pulumi:tags:
    value:
      pulumi:template: azure-go
