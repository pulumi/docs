name: sample-components-typescript
description: A Pulumi TypeScript program that builds and uses a static website component
runtime: nodejs
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
