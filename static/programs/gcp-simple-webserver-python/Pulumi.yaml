name: gcp-simple-webserver-python
description: A example that deploys the resources for a simple web server on Google Cloud.
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
