name: gcp-import-service-account-javascript
description: An example that deploys the configuration necessary to import a Service Account resource in Google Cloud.
runtime:
  name: nodejs
  options:
    typescript: false
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-javascript
