#!/bin/bash

# Only show on initial clone (not branch switches)
if [ "$3" = "1" ] && [ ! -f .git/.claude-welcome-shown ]; then
  echo ""
  echo "💡 Tip: Run /docs-tools in Claude Code or GitHub Copilot to see available custom Pulumi Docs commands."
  echo ""
  touch .git/.claude-welcome-shown
fi
