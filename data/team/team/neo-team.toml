id = "neo-team"
name = "Pulumi Neo Team"
title = "Product Team"
status = "active"

# This byline is a group, not a natural person -- schema.org Person denotes an
# individual, so layouts/partials/schema/utils/author-entities.html reads this
# flag and emits JSON-LD "@type": "Organization" instead.
entity_type = "organization"