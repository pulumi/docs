name: aws-simulated-server-interpolate-python
runtime:
  name: python
  options:
    virtualenv: venv
description: An example that creates a simulated webserver and interpolates the value of the URL
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
