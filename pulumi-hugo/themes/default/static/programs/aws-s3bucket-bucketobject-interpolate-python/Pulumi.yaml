name: aws-s3bucket-bucketobject-interpolate-python
runtime:
  name: python
  options:
    virtualenv: venv
description: An example that creates an S3 bucket and bucket object
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
