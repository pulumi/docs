import { DateCountdownCircles } from "./date-countdown-circles";

describe("pulumi-date-countdown-circles", () => {
    it("builds", () => {
        expect(new DateCountdownCircles()).toBeTruthy();
    });
});
